using RepManagement.Models;
using System;

namespace RepManagement.TestDB
{
    class Program
    {
        static void Main(string[] args)
        {
            //using (RepManagementContext context=new RepManagementContext())
            //{
            //    context.Database.EnsureCreated();

            //    context.Roles.Add(new Role { ID = Guid.NewGuid(), RoleName = "管理员" });
            //    context.Users.Add(new User { ID=Guid.NewGuid(),UserName="admin",Password="admin",DisplayName="管理员" });

            //    context.SaveChanges();
            //}
        }
    }
}
